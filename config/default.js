/* eslint-env node */
module.exports = {
	client: {
		// log in and obtain a token via: https://cloud.lifx.com/sign_in
		// then, copy this file to local.js, and fill in client.secret
		// another option is to set process.env.LIFX_CLIENT_SECRET=...
	},
}
